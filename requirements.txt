# JML Automation - Python dependencies for employee lifecycle management

# Core dependencies
pyyaml>=6.0
requests>=2.31.0
urllib3>=2.0.0

# CLI interface
click>=8.1.0

# Microsoft Graph API support
msal>=1.24.0

# Google API libraries
google-api-python-client>=2.100.0
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Date/time utilities
python-dateutil>=2.8.2

# For better logging
colorlog>=6.7.0

# Optional but recommended
python-dotenv>=1.0.0  # For .env file support

# 1Password CLI integration (ensure op CLI is installed separately)
# No Python package needed for 1Password CLI - uses subprocess